server:
  port: 2008

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/DevRooster/EXAMEN-PARCIAL-DAD.git #Tu reposritorio
          searchPaths: config-data #Crearemos esta carpeta
          default-label: gwclc #Rama de tu repositorio

  security:
    user:
      name: root
      password: 123456